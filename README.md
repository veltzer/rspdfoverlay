# rspdfoverlay

Overlay data on PDF files.

A desktop tool for placing text, dates, signatures and images onto existing
PDF documents and writing the result back into the file, fully offline. The
design is described in `DESIGN.md`; the implementation is at an early stage.

Documentation lives in the `docs/` mdBook.
